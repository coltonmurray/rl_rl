[Locations]
looks_config = ./appearance.cfg
python_file = ./rlbot_bot.py

name = RL_RL_TouchV2
maximum_tick_rate_preference = 120

[Details]
developer = Colton Murray
description = Local PPO checkpoint trained with RLGym/RocketSim and loaded through a small RLBot adapter.
fun_fact = Uses the same 90-action RLGym lookup table as training.
github =
language = python
tags = 1v1, rlgym, ppo
